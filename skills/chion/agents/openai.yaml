interface:
  display_name: "Chion"
  short_description: "让独立三角色优先复用、自动解决问题，只把业务决定交给用户"
  default_prompt: "Use $chion to run formal development with separate PM, Worker, and Reviewer roles that reuse proven solutions, solve technical problems automatically, and ask the user only for real business or authorization decisions."
