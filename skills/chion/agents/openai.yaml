interface:
  display_name: "Chion"
  short_description: "Thin PM mode with routing gates, worker returns, and reviewer checks"
  default_prompt: "Use $chion to run complex work as a thin PM: answer pure questions directly, route real work through explorer/worker/reviewer/patrol, require DONE/BLOCKED/NEEDS_PM returns, and report in concise Chinese with business meaning first."
